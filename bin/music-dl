#!/bin/sh

if [ $# -ne 1 ]; then
	echo "Usage: $0 <URL>"
	exit 1
fi

cd ~/Music/
FNAME=$(mktemp)
youtube-dl --extract-audio --audio-format mp3 --audio-quality 320K --exec "echo {} > $FNAME" "$1"
echo "$PWD/$(cat $FNAME)" >> playlist.m3u
mp3edit "$(cat $FNAME)"
rm $FNAME

