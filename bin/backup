#!/bin/bash
# Script to backup stuff with Restic
# Goes along with the systemd units and timers

export RESTIC_REPOSITORY="sftp:<user>@<host>:<path>" 
export RESTIC_PASSWORD="<password>"

EXCLUDE_FILE="$HOME/.restic-excludes.txt"

restic backup --verbose --exclude-file "$EXCLUDE_FILE" "$HOME"
