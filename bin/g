#!/usr/bin/env python3

# Script to open a search on Google in the browser

import os
import sys
import urllib.parse 

browser = 'firefox'
query = urllib.parse.quote(' '.join(sys.argv[1:]))
os.system(f'{browser} https://google.com/search?q={query}')
