#!/bin/bash

# Write a iso image to a device

if [[ $# -ne 2 ]]; then
    echo "Usage: writeimg image.iso /dev/sdX"
    exit 1
fi

echo -n "Writing $1 to $2. Continue? (y/n) "
read confirm
if [[ "$confirm" = "y" ]]; then
    sudo sh -c "/usr/local/bin/pv <$1 >$2"
else
    echo "Aborting"
fi

